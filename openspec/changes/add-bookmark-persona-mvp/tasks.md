## 1. 项目搭建

- [x] 1.1 在仓库根目录搭建 Next.js（App Router，TypeScript）应用
- [x] 1.2 添加依赖：Anthropic SDK、客户端图片导出库（html-to-image）、ID 生成器（nanoid）、存储客户端（Redis 或 SQLite）
- [x] 1.3 以仓库根 `.env.example` 为配置单一来源（已创建），部署时复制为 `.env` 并填 `ANTHROPIC_API_KEY`
- [x] 1.4 新增类型化配置模块读取 `.env`，集中校验所有限值（`DAILY_RUN_BUDGET`、`PER_RUN_MAX_TOKENS`、条目上限、抓取/迭代/墙钟边界、限流、`CARD_TTL_DAYS` 等），缺失给安全默认

## 2. 书签导入（客户端）

- [x] 2.1 实现拖拽/上传 UI，仅接受单个 HTML 文件
- [x] 2.2 在浏览器内实现 Netscape 书签 HTML 解析器 → {title,url,domain,folderPath[],addDate}
- [x] 2.3 条目归一化 + 去重（合并重复 URL、记录所有文件夹路径、剔除/修复损坏项）
- [x] 2.4 实现到上限的分层下采样；提示「已使用代表性样本」
- [x] 2.5 非书签/损坏文件以清晰提示拒绝，且零网络请求

## 3. 人格 agent（服务端）

- [x] 3.1 定义画像输出 JSON schema（带规模命名簇、3-7 特质、演变时间线）及校验器
- [x] 3.2 实现本地 `get_overview()`（计数、域名直方图、时间跨度、文件夹树摘要）——非 LLM 调用
- [x] 3.3 实现一次性 LLM 聚类调用（Haiku），返回命名簇 + 人格草图
- [x] 3.4 实现 `fetch_page(url)` 工具：仅公网 http/https，屏蔽内网/环回/链路本地，不跟随跳向内网的重定向，超时、体积上限、正文抽取
- [x] 3.5 实现 Anthropic 工具调用 agent 循环，带边界（最大抓取=8、最大迭代、墙钟、总 token 预算）及抓取失败时优雅降级
- [x] 3.6 实现 `finalize_profile` 合成（Sonnet），带 schema 校验 + 语气/安全 system prompt；无效/不安全输出则重生成
- [x] 3.7 以服务端路由暴露 agent；确保任何 Key/凭据都不到达客户端
- [x] 3.8 实现按风格重合成：复用缓存的概览+簇，校验风格参数，仅一次合成调用，不重跑深挖

## 4. API 安全护栏

- [x] 4.1 按 IP 滑动窗口限流 + 并发运行上限（在任何模型调用前拒绝）
- [x] 4.2 在 LLM 调用前强制请求体积/条目数上限
- [x] 4.3 全局每日熔断：按当天完成运行数计数，达 `DAILY_RUN_BUDGET`（默认 100）→ 返回「临时暂停」、站点仍在线、次日自动恢复；并对单次运行强制 `PER_RUN_MAX_TOKENS`
- [x] 4.4 `AGENT_ENABLED` 特性开关 → 「已暂停」状态且不破坏前端
- [x] 4.5 通俗数据处理说明页/区块接入 UI

## 5. 人格卡 + 分享

- [x] 5.1 构建人格卡组件（特质、带相对规模的簇、演变时间线、固定 AI 免责声明）
- [x] 5.2 客户端图片导出（下载），无额外服务端调用
- [x] 5.3 仅存派生画像 JSON，自托管在 ECS（Redis 原生 TTL 或 SQLite + expires_at 清扫），随机 nanoid 键，7 天 TTL；生成分享链接
- [x] 5.4 分享路由按 ID 渲染卡片；过期/未知 ID → 友好状态
- [x] 5.5 「换个风格重新生成」UI 控件，调用 3.8，每个变体生成独立分享链接
- [x] 5.6 渐进式 UI：流式输出 概览 → 簇 → 最终卡片，掩盖 agent 时延

## 6. 验证

- [x] 6.1 测试夹具：小、超上限、大量重复、损坏 四类书签导出
- [x] 6.2 逐条验证 spec 场景（导入、agent 边界 + SSRF 拒绝 + 优雅降级、按风格重合成、卡片/分享/过期、限流 + 熔断 + 开关）
- [x] 6.3 确认客户端包/网络无凭据；确认原始文件绝不上传；确认仅存派生画像
- [x] 6.4 端到端真实跑通（DashScope/qwen3.5-plus，scripts/smoke.ts，6 书签 167.9s 成功）；⚠️ 时延超 Next 路由 maxDuration，待优化
